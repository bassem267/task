pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Ensure correct path separator for Windows
                bat 'dir.bat'
            }
        }
    }
}
